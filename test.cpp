#include "utils.hpp"

using namespace std;
using namespace arma;

int main(){
    int M = 10;
    // A.print();
    // B.print();
    // cx_vec V = potential(M);
    // u_init(M, 0.25, 0.05 ,200, 0.5, 0.05, 0, V);

    // potential(61);

    return 0;
}